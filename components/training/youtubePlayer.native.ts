import type { ComponentType } from 'react';
import Player from 'react-native-youtube-iframe';

/** Native resolution of the drill video player: YouTube's official IFrame API. */
export const YoutubePlayer: ComponentType<any> = Player;
